# Custom log format (overrides the image's default) — JSON access log with full request body,
# so every request (not just 4xx/5xx) is captured with its payload for the security log pipeline.
log_format main escape=json '{"remote_addr":"$realip_remote_addr","remote_user":"$remote_user","time":"$time_iso8601","method":"$request_method","path":"$request_uri","status":$status,"body_bytes_sent":$body_bytes_sent,"http_referer":"$http_referer","http_user_agent":"$http_user_agent","http_x_forwarded_for":"$http_x_forwarded_for","request_body":"$request_body","headers":"$all_headers"}';

access_log ${ACCESSLOG} main;
access_log ${METRICSLOG} combined;

error_log ${ERRORLOG} ${LOGLEVEL};
